export const useCart = defineStore("cart", ({
    state: () => ({
        carts: []
    }),
    actions: {
        getCarts: async () => {
            // const { data } = await useApi("")
        }
    }
}))